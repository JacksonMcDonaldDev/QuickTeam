services:
  mongo:
    image: mongo:4.4
    container_name: quickteam-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
